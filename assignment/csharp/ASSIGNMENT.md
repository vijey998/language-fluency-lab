# C# checkpoints
1. Inspect the solution, project references, file-scoped namespaces, implicit/global usings, records, immutable collections, and tests.
2. Add catalog extension methods for sorting/grouping and one iterator using `yield return`.
3. Implement DependencyGraph.
4. Implement generic LRU and bounded queue.
5. Add strategy interfaces and register implementations through a small composition-root/DI module.
6. Add JSON persistence with System.Text.Json and custom enum/date options.
7. Add async worker execution, cancellation, timeout, and IAsyncDisposable cleanup.
8. Add a Minimal API project referencing Core; wire handlers and configuration.
9. Add a second class library and practice `internal`, `InternalsVisibleTo`, and project references.
