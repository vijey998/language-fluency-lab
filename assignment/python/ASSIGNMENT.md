# Python checkpoints
1. Inspect `models.py`, package exports, `pyproject.toml`, and tests. Add one model test.
2. Extend `catalog.py` with prefix indexing via a trie and one generator-based query.
3. Implement `graph.py`.
4. Complete `StablePriorityQueue`, `Trie`, and `BoundedQueue`.
5. Implement JSON persistence.
6. Refactor engine handlers behind a `Protocol`; add a decorator that records duration.
7. Add an asyncio execution mode and cancellation test.
8. Add an API package containing framework-neutral handlers; optionally adapt to FastAPI.
