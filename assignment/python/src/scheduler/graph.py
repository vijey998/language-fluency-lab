"""Checkpoint 3: dependency graph.

Implement this module from the behavioral specification. Use a map for indegrees,
a map-of-lists for outgoing edges, and a deque for Kahn's algorithm. Ensure output
is deterministic and reject both missing dependencies and cycles.
"""
# Deliberately empty: model your public API after usages in tests and engine.py.
