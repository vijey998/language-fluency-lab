"""Checkpoint 5: JSON persistence.

Create JsonTaskRepository(path) with save(tasks) and load(). Preserve enums,
datetimes, frozensets, and the read-only payload mapping. Use pathlib and json.
The solution should round-trip equality-relevant fields.
"""
