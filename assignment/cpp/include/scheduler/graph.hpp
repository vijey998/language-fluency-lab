#pragma once
#include "catalog.hpp"
#include <vector>
namespace scheduler {
// Checkpoint 3: declare DependencyGraph with constructor, topological_order(),
// and ready_ids(). Then implement it in src/graph.cpp using Kahn's algorithm.
// Deliberately no class body: infer call sites from engine.cpp and tests.
}
