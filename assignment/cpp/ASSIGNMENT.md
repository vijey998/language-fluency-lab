# C++ checkpoints
1. Build the solution, inspect header/source separation, namespaces, target linking, and public include paths.
2. Extend Task with ordering helpers and add algorithms using ranges, lambdas, structured bindings, optional, and variant.
3. Implement DependencyGraph from its contract-only files.
4. Implement template structures in the header: stable heap, LRU, bounded queue.
5. Add unique_ptr-owned polymorphic task handlers and a factory returning unique_ptr.
6. Add RAII subscription and scoped timer types; exercise copy/move deletion/defaulting.
7. Add jthread/stop_token worker execution and exception-safe shutdown.
8. Add JSON or SQLite persistence behind an abstract repository.
9. Split one library into an additional CMake target and link it into CLI/tests.
