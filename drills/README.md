# Timed drills

Perform each in all three languages. Commit after every drill.

1. **5 min:** Add a `deadline` field without breaking serialization compatibility.
2. **8 min:** Add sorting by estimated duration with a custom comparator/key selector.
3. **10 min:** Add `GET task by prefix` through the trie and API boundary.
4. **12 min:** Add a `RetryPolicy` strategy with exponential backoff calculation.
5. **12 min:** Add a plugin task handler named `ChecksumTaskHandler`.
6. **15 min:** Add cancellation propagation from scheduler to worker.
7. **15 min:** Add an LRU-cached catalog lookup and prove eviction in a test.
8. **20 min:** Add SQLite persistence behind the existing repository interface.
9. **20 min:** Add one async API endpoint/client call and timeout handling.
10. **30 min:** Cold-rewrite models, catalog, dependency graph, and priority queue in a blank folder.

Rules: no autocomplete for the first attempt; syntax reference allowed only after three minutes; solution comparison only after the timer expires.
